# PatchMox Environment Configuration
# Copy this file to .env and customize as needed

# ============================================================================
# LOGGING CONFIGURATION
# ============================================================================

# Directory where log files will be stored
# Default: <script_directory>/logs
# LOG_DIR=/var/log/patchmox

# Logging level: debug, info, notice, warn, error, critical, alert, emergency
# Default: info
# LOG_LEVEL=info

# Console logging. By default PatchMox only logs to file and journal to keep
# cron/timer output clean. Set this to "true" to also print to the terminal
# when running interactively.
# Default: false
# PATCHMOX_CONSOLE_LOG=true

# Log message format
# Available variables:
#   %d = date and time (YYYY-MM-DD HH:MM:SS)
#   %z = timezone (UTC or LOCAL)
#   %l = log level name (DEBUG, INFO, WARN, ERROR, etc.)
#   %s = script name
#   %m = message
# Default: [%d] [%l] %m
# LOG_FORMAT="[%d] [%l] %m"

# ============================================================================
# VERSION FETCH CACHE CONFIGURATION
# ============================================================================

# PatchMox caches the latest VirtIO and QEMU GA versions from the Fedora
# People Archive to avoid repeated network requests when running checks
# frequently (e.g. cron every few minutes).

# How long the cached versions remain valid, in minutes.
# Default: 60
# VERSION_CACHE_TTL_MINUTES=60

# Force a fresh online fetch on the next check, ignoring the cache.
# Default: false
# VERSION_CACHE_FORCE_REFRESH=true

# ============================================================================
# GUEST AGENT CACHE CONFIGURATION
# ============================================================================

# PatchMox can skip guest-agent version queries for VMs that were recently
# checked and have not changed (same vmgenid, still running). This is the
# second-level cache and removes most of the runtime on unchanged daily checks.

# How long cached guest-agent versions remain valid, in minutes.
# Default: 60
# GUEST_AGENT_CACHE_TTL_MINUTES=60

# Force a fresh guest-agent query on the next check, ignoring the cache.
# Can also be triggered per-run with `patchmox check --force-refresh`.
# Default: false
# GUEST_AGENT_CACHE_FORCE_REFRESH=true

# ============================================================================
# SVG NOTIFICATION CONFIGURATION
# ============================================================================


# Path to the SVG template file
# Default: <script_directory>/templates/svg/update-nag-template.svg
# SVG_IMAGE_TEMPLATE=/path/to/custom/template.svg

# ============================================================================
# NOTIFICATION CONFIGURATION
# ============================================================================

# Notification channels (comma-separated list). Supported values: smtp, msgraph, webhook
# Example: enable SMTP and webhook:
# NOTIFICATION_CHANNELS=smtp,webhook

# E-Mail Based Notifications:

# Email notification settings (SMTP)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=notifications@example.com
# SMTP_PASSWORD=your_password
# SMTP_FROM=pve-updater@example.com
#
# Recipients. MAIL_TO can be:
#   - a comma-separated string (recommended for .env): MAIL_TO="admin@example.com, support@example.com"
#   - a Bash array declared in .env (works in most shells): MAIL_TO=("admin@example.com" "support@example.com")
#   - a single address: MAIL_TO="admin@example.com"
#
# TLS is automatically derived from SMTP_PORT (standard IANA assignments):
#   25  -> opportunistic STARTTLS (smtp:// --ssl)
#   587 -> required STARTTLS     (smtp:// --ssl-reqd)   [default port]
#   465 -> implicit TLS          (smtps://)

# Email notification settings (Microsoft Graph)
# MSGRAPH_TENANT_ID=your_tenant_id
# MSGRAPH_CLIENT_ID=your_client_id
# MSGRAPH_CLIENT_SECRET=your_client_secret
# MSGRAPH_FROM=patchmox@example.com

# Webhook notification settings (outgoing — sends a POST to an external URL when updates are detected)
# WEBHOOK_URL=https://hooks.example.com/your-webhook
# WEBHOOK_METHOD=POST
# WEBHOOK_HEADERS='{"Content-Type": "application/json"}'

# ============================================================================
# WEBHOOK SERVER CONFIGURATION (inbound — adnanh/webhook)
# Runs a local HTTP server on the Proxmox host. Clicking the SVG nag banner
# in the Proxmox UI fires a request here, triggering vm-update.sh for that VM.
# This is separate from WEBHOOK_URL above, which sends outgoing notifications.
# ============================================================================

# Enable the webhook server integration (true/false)
# WEBHOOK_ENABLED=true

# IP or hostname of this Proxmox host — used to build clickable URLs in VM descriptions
# WEBHOOK_HOST=192.168.1.1

# Port the webhook binary listens on (default: 9000)
# WEBHOOK_PORT=9000

# Token appended as ?token= to authenticate click requests — change from the default!
# WEBHOOK_SECRET=changeme

# Systemd service name to restart after hooks.json changes
# WEBHOOK_SERVICE=patchmox-webhook.service

# ============================================================================
# PROGRESS / INTERACTIVE UI CONFIGURATION
# ============================================================================

# Disable interactive spinner/progress animations in the terminal.
# Useful for cron, systemd timers, or non-interactive shells.
# Default: unset (enabled on TTYs)
# PATCHMOX_NO_PROGRESS=1

# Where progress output is written: 1 = stdout, 2 = stderr
# Default: 2 (stderr)
# PATCHMOX_PROGRESS_TARGET=2

# ============================================================================
# ADVANCED CONFIGURATION FOR LOGGING (I recommend leaving these as defaults)
# ============================================================================

# Use UTC time in logs (true/false)
# Default: false
# USE_UTC=false

# Use systemd journal for logging (true/false)
# Default: true
# USE_JOURNAL=true

# Tag for systemd journal entries
# Default: PatchMox
# JOURNAL_TAG=PatchMox

# Color settings for console output: auto, always, never
# Default: auto
# USE_COLORS=auto

# Environment variables for NO_COLOR and CLICOLOR standards
# NO_COLOR=1          # Set to disable colors
# CLICOLOR=0          # Set to 0 to disable colors
# CLICOLOR_FORCE=1    # Set to 1 to force colors
